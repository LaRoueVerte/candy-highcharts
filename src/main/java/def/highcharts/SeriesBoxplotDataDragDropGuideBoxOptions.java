package def.highcharts;
/**
 * (Highcharts) Style options for the guide box. The guide box has one state by
 * default, the `default` state.
 */
@jsweet.lang.Interface
public abstract class SeriesBoxplotDataDragDropGuideBoxOptions extends def.js.Object {
    /**
     * (Highcharts) Style options for the guide box default state.
     */
    @jsweet.lang.Name("default")
    @jsweet.lang.Optional
    public DragDropGuideBoxOptionsObject Default;
}

