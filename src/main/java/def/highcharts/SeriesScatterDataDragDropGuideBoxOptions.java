package def.highcharts;
/**
 * (Highcharts, Highstock) Style options for the guide box. The guide box has
 * one state by default, the `default` state.
 */
@jsweet.lang.Interface
public abstract class SeriesScatterDataDragDropGuideBoxOptions extends def.js.Object {
    /**
     * (Highcharts, Highstock) Style options for the guide box default state.
     */
    @jsweet.lang.Name("default")
    @jsweet.lang.Optional
    public DragDropGuideBoxOptionsObject Default;
}

